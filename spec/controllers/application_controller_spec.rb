require 'spec_helper'

describe ApplicationController do

  #describe "#authenticate_admin_user!" do
    #describe "when the user isn't authenticated" do
      #before(:each) do
        #controller.should_receive(:current_user).and_return(nil)
      #end

      #it "should display a flash message" do
        #controller.send :authenticate_admin_user!
        #flash[:notice].should eq("You must be admin to access admin pages.")
      #end

      #it "redirect to the signin page" do
        #controller.send :authenticate_admin_user!
        #response.should redirect_to(signin_path)
      #end

    #end
  #end

end
